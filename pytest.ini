[pytest]
markers =
    integration: Integration tests that require network sockets (skip by default)
    asyncio: Async tests that require pytest-asyncio

# Filter out integration tests from default test run
addopts = -m "not integration"
